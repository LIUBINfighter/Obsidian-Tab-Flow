name: CodeQL + ESLint security and code-smell checks

on:
  push:
    branches: [ dev, master ]
  pull_request:
    branches: [ dev, master ]
  workflow_dispatch:      

jobs:
  codeql-analysis:
    name: CodeQL Analysis
    runs-on: ubuntu-latest
    permissions:
      actions: read
      security-events: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v2
      with:
        languages: javascript
        # customize queries path to include our custom queries
        queries: ./.github/codeql-custom-queries

    - name: Autobuild
      uses: github/codeql-action/autobuild@v2

    - name: Run CodeQL database analysis
      uses: github/codeql-action/analyze@v2
      with:
        category: "security-and-quality"

#   eslint:
#     name: ESLint
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v4

#     - name: Setup Node.js
#       uses: actions/setup-node@v4
#       with:
#         node-version: '18'

#     - name: Install dependencies
#       run: npm ci

#     - name: Run ESLint
#       run: npx eslint "src/**/*.ts" "src/**/*.js" || true
